# Rust desktop client
rust_desktop/target/

# Android project
android/.gradle/
android/.idea/
android/.kotlin/
android/**/build/
android/local.properties
# Android Studio「Generate Signed APK」的默认输出目录：是构建产物，不是源码
android/app/release/
*.iml

# Release binaries (except the two tracked below)
*.exe
*.apk
*.aab
*.keystore
*.jks
!CrossClip.exe
!CrossClip.apk

# Runtime data / private local config (never commit)
config.json
*.log

# OS noise
.DS_Store
Thumbs.db

# IDE / local tooling state (never commit)
.idea/
.reasonix/
.xcodemap/
