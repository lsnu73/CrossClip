<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.crossclip.app">

    <!-- 网络与局域网广播权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <!-- 前台服务与保活权限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- 文件保存权限：仅 Android 9 及以下需要真实的外部存储写权限；
         Android 10+ 写 Download 目录走系统豁免路径，自定义目录走 SAF 授权（无需权限） -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Android 11+ 包可见性：queryIntentActivities 只能看到此处声明匹配的应用。
         SaveDirManager.findDocumentsUi 需要按「VIEW + content + 目录 MIME」查询系统
         文档组件（DocumentsUI），不声明的话查询结果在系统侧被裁剪、永远为空。
         用户可见的「打开方式」候选枚举发生在系统 resolver，不受此声明影响。 -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data
                android:scheme="content"
                android:mimeType="vnd.android.document/directory" />
        </intent>
    </queries>

    <application
        android:name=".CrossClipApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:requestLegacyExternalStorage="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.CrossClip"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 屏 2 · 电脑配对：设备发现列表 + 手动添加 IP -->
        <activity
            android:name=".ui.PairingActivity"
            android:exported="false" />

        <!-- 屏 3 · 输入 PIN 码：6 格配对码 -->
        <activity
            android:name=".ui.PairCodeActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize|stateHidden" />

        <!-- 屏 4 · 诊断与排查工具 -->
        <activity
            android:name=".ui.DiagnosticsActivity"
            android:exported="false" />

        <!-- 后台透明剪贴板写入器 (解决 Android 10+ 剪贴板受限降级兼容) -->
        <activity
            android:name=".ui.ClipWriteActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:launchMode="singleInstance" />

        <!-- 「文件接收完成」通知的点击目标：打开接收文件保存目录后立即自我结束。
             必须由 Activity 承接，因为保存目录的打开逻辑需要 Activity 上下文才能调起文件管理器。 -->
        <activity
            android:name=".ui.OpenSaveDirActivity"
            android:exported="false"
            android:theme="@style/Theme.CrossClip.Transparent"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity=""
            android:launchMode="singleInstance" />

        <!-- Shizuku 服务绑定 Provider -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <!-- 日志文档分享 FileProvider -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- 前台同步守护服务 -->
        <service
            android:name=".service.SyncForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="connectedDevice|dataSync" />

        <!-- 控制中心快捷磁贴。图标与应用图标同源：磁贴显示的是彩色图标，
             与通知小图标走的是两条不同的渲染路径 —— 后者必须用图案层（含 alpha 蒙版），
             见 SyncForegroundService.NOTIFICATION_SMALL_ICON 的说明。 -->
        <service
            android:name=".tile.SendClipTileService"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- 系统文本长按选中菜单 -->
        <activity
            android:name=".receiver.ProcessTextActivity"
            android:label="@string/process_text_label"
            android:theme="@style/Theme.CrossClip.Transparent"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <!-- 文件分享接收 Activity：接收其他应用分享的文件并发送到电脑 -->
        <activity
            android:name=".receiver.ShareReceiveActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.CrossClip.Transparent"
            android:exported="true"
            android:launchMode="singleTask"
            android:excludeFromRecents="true">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
        </activity>

    </application>

</manifest>
